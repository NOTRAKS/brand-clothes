# 🚀 Инструкция по деплою на ваш хостинг

## Шаг 1: Подготовка проекта локально

### 1.1. Соберите проект для продакшена

Откройте терминал в папке проекта и выполните:

```bash
# Установите зависимости (если еще не установлены)
npm install

# Соберите проект
npm run build
```

Если сборка прошла успешно без ошибок - проект готов к деплою!

---

## Шаг 2: Определите тип вашего хостинга

### Вариант A: VPS с SSH доступом (Linux сервер)
- У вас есть доступ по SSH (через терминал)
- Можете устанавливать Node.js, PM2, Nginx
- **Переходите к разделу "Деплой на VPS"**

### Вариант B: Shared Hosting (обычный хостинг с панелью)
- Доступ только через FTP/SFTP или панель управления (cPanel, ISPmanager и т.д.)
- Нет SSH доступа или ограниченный
- **Переходите к разделу "Деплой на Shared Hosting"**

### Вариант C: Хостинг с поддержкой Node.js
- В панели есть раздел "Node.js" или "Приложения"
- Можете указать команду запуска
- **Переходите к разделу "Деплой на Node.js хостинг"**

---

## 📦 Деплой на VPS (с SSH доступом)

### 2.1. Подключитесь к серверу

```bash
ssh ваш_пользователь@IP_вашего_сервера
# или
ssh ваш_пользователь@ваш_домен.com
```

### 2.2. Установите Node.js (если еще не установлен)

```bash
# Проверьте версию Node.js
node -v

# Если Node.js не установлен, установите (Ubuntu/Debian):
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Или для CentOS/RHEL:
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs
```

### 2.3. Загрузите проект на сервер

**Способ 1: Через Git (рекомендуется)**

```bash
# На сервере создайте папку для проекта
mkdir -p ~/blck-ltd
cd ~/blck-ltd

# Если проект в Git репозитории:
git clone https://github.com/ваш-username/ваш-репозиторий.git .

# Если нет Git репозитория, используйте способ 2
```

**Способ 2: Через SFTP/FTP**

1. Используйте FileZilla, WinSCP или другой FTP клиент
2. Подключитесь к серверу
3. Загрузите все файлы проекта в папку (например, `~/public_html` или `~/blck-ltd`)
4. **НЕ загружайте:** `node_modules`, `.next`, `.git`

### 2.4. Установите зависимости и соберите проект

```bash
cd ~/blck-ltd  # или путь к вашему проекту

# Установите зависимости
npm install --production

# Соберите проект
npm run build
```

### 2.5. Установите PM2 для управления процессом

```bash
# Установите PM2 глобально
sudo npm install -g pm2

# Запустите проект через PM2
pm2 start npm --name "blck-ltd" -- start

# Сохраните конфигурацию PM2
pm2 save

# Настройте автозапуск при перезагрузке сервера
pm2 startup
# Выполните команду, которую покажет PM2
```

### 2.6. Настройте Nginx как прокси

```bash
# Установите Nginx (если еще не установлен)
sudo apt-get update
sudo apt-get install nginx  # для Ubuntu/Debian
# или
sudo yum install nginx      # для CentOS/RHEL

# Создайте конфигурационный файл
sudo nano /etc/nginx/sites-available/blck.ltd
```

Вставьте следующую конфигурацию (замените `blck.ltd` на ваш домен):

```nginx
server {
    listen 80;
    server_name blck.ltd www.blck.ltd;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X)

```bash
# Активируйте конфигурацию
sudo ln -s /etc/nginx/sites-available/blck.ltd /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезагрузите Nginx
sudo systemctl reload nginx
```

### 2.7. Настройте SSL (HTTPS) с Let's Encrypt

```bash
# Установите Certbot
sudo apt-get install certbot python3-certbot-nginx  # для Ubuntu/Debian
# или
sudo yum install certbot python3-certbot-nginx      # для CentOS/RHEL

# Получите SSL сертификат
sudo certbot --nginx -d blck.ltd -d www.blck.ltd

# Следуйте инструкциям на экране
# Certbot автоматически настроит HTTPS и обновит Nginx
```

### 2.8. Настройте DNS записи

В панели управления доменом добавьте:

```
Тип: A
Имя: @
Значение: IP_вашего_VPS_сервера
TTL: 3600

Тип: A
Имя: www
Значение: IP_вашего_VPS_сервера
TTL: 3600
```

---

## 🌐 Деплой на Shared Hosting (обычный хостинг)

### 3.1. Подготовьте файлы для загрузки

На вашем компьютере выполните:

```bash
# Соберите проект
npm run build

# Создайте архив без ненужных файлов
# (или просто загрузите через FTP, исключая node_modules и .next)
```

### 3.2. Загрузите файлы через FTP/SFTP

1. Откройте FileZilla, WinSCP или другой FTP клиент
2. Подключитесь к хостингу (данные должны быть в письме от хостинга)
3. Загрузите файлы в папку `public_html` или `www` (или указанную хостингом)
4. **Загрузите:**
   - Все файлы из корня проекта (package.json, next.config.mjs и т.д.)
   - Папку `app/`
   - Папку `components/`
   - Папку `public/`
   - Папку `styles/`
   - Файлы конфигурации
5. **НЕ загружайте:**
   - `node_modules/` (установится на сервере)
   - `.next/` (соберется на сервере)
   - `.git/`

### 3.3. Установите зависимости на сервере

**Если есть SSH доступ:**
```bash
cd ~/public_html  # или путь к проекту
npm install --production
npm run build
```

**Если нет SSH, но есть панель управления:**
1. Найдите раздел "Node.js" или "Приложения" в панели
2. Создайте новое Node.js приложение
3. Укажите:
   - **Путь к проекту:** `/public_html` (или где загрузили файлы)
   - **Команда запуска:** `npm start`
   - **Команда установки:** `npm install --production`
   - **Команда сборки:** `npm run build`
   - **Порт:** `3000` (или указанный хостингом)

### 3.4. Настройте домен в панели хостинга

1. Войдите в панель управления хостингом
2. Найдите раздел "Домены" или "Domains"
3. Добавьте ваш домен `blck.ltd`
4. Укажите папку проекта (где загрузили файлы)

### 3.5. Настройте проксирование (если нужно)

Если хостинг требует настройки проксирования в `.htaccess` или конфигурации:

Создайте файл `.htaccess` в корне проекта (для Apache):

```apache
RewriteEngine On
RewriteRule ^(.*)$ http://localhost:3000/$1 [P,L]
```

Или настройте проксирование в панели хостинга (если есть такая опция).

---

## 🟢 Деплой на Node.js хостинг (с панелью управления)

### 4.1. Загрузите проект через FTP/SFTP

См. раздел 3.2 выше.

### 4.2. Создайте Node.js приложение в панели

1. Войдите в панель управления хостингом
2. Найдите раздел "Node.js" или "Приложения"
3. Нажмите "Создать приложение" или "Add Application"
4. Заполните:
   - **Название:** `blck-ltd`
   - **Путь:** `/public_html` (или где загрузили файлы)
   - **Node.js версия:** `18.x` или `20.x`
   - **Команда запуска:** `npm start`
   - **Порт:** `3000` (или автоматически)
   - **Переменные окружения:**
     - `NODE_ENV=production`
     - `PORT=3000`

### 4.3. Установите зависимости и соберите

В панели найдите кнопку "Установить зависимости" или выполните команды:

```bash
npm install --production
npm run build
```

### 4.4. Запустите приложение

В панели нажмите "Запустить" или "Start"

### 4.5. Настройте домен

1. В настройках Node.js приложения найдите "Домены"
2. Добавьте домен `blck.ltd`
3. Хостинг автоматически настроит проксирование

---

## ✅ Проверка работы

### После деплоя проверьте:

1. **Откройте сайт:** `https://blck.ltd` (или `http://` если SSL еще не настроен)
2. **Проверьте все страницы:**
   - Главная страница
   - Страница коллекций
   - Страница checkout
3. **Проверьте форму заказа:**
   - Заполните форму
   - Нажмите "Перейти к оплате"
   - Должен открыться Telegram

---

## 🔧 Полезные команды для управления

### Если используете PM2:

```bash
# Посмотреть статус
pm2 status

# Посмотреть логи
pm2 logs blck-ltd

# Перезапустить
pm2 restart blck-ltd

# Остановить
pm2 stop blck-ltd

# Удалить из PM2
pm2 delete blck-ltd
```

### Если используете обычный запуск:

```bash
# Запустить
npm start

# Остановить
# Нажмите Ctrl+C в терминале
```

---

## ❓ Частые проблемы

### Сайт не открывается

1. Проверьте, что приложение запущено (`pm2 status` или в панели)
2. Проверьте логи на ошибки
3. Проверьте DNS записи (может занять до 24 часов)
4. Проверьте, что порт 3000 открыт в firewall

### Ошибка "Cannot find module"

1. Убедитесь, что выполнили `npm install --production`
2. Проверьте, что `node_modules` установлен
3. Пересоберите проект: `npm run build`

### Изображения не загружаются

1. Убедитесь, что папка `public/` загружена на сервер
2. Проверьте пути к изображениям в коде

### Форма не работает / Telegram не открывается

1. Убедитесь, что сайт работает по HTTPS (Telegram требует HTTPS)
2. Проверьте консоль браузера на ошибки (F12)

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи приложения
2. Проверьте логи веб-сервера (Nginx/Apache)
3. Обратитесь в поддержку хостинга с описанием проблемы

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт должен быть доступен на вашем домене!


