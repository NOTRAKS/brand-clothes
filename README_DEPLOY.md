# 🚀 БЫСТРАЯ ИНСТРУКЦИЯ: Деплой на REG.RU

## 📦 Шаг 1: Создайте ZIP архив

### Вариант A: Автоматически (Mac/Linux)
```bash
./create-zip.sh
```

### Вариант B: Вручную
1. Выделите эти папки и файлы:
   - `app/`
   - `components/`
   - `public/`
   - `styles/`
   - `lib/` (если есть)
   - `hooks/` (если есть)
   - `package.json`
   - `package-lock.json`
   - `next.config.mjs`
   - `tsconfig.json`
   - `postcss.config.mjs`
   - `components.json` (если есть)
   - `.gitignore`

2. Создайте ZIP архив
3. **НЕ включайте:** `node_modules/`, `.next/`, `.git/`

---

## 🌐 Шаг 2: Загрузите на REG.RU

1. Войдите в панель REG.RU
2. Откройте **"Файловый менеджер"**
3. Перейдите в `public_html`
4. Загрузите ZIP архив
5. Распакуйте архив

---

## ⚙️ Шаг 3: Настройте Node.js приложение

1. В панели найдите **"Node.js"** или **"Приложения"**
2. Создайте новое приложение:
   - **Путь:** `/public_html`
   - **Node.js версия:** `18.x` или `20.x`
   - **Команда запуска:** `npm start`
   - **Порт:** `3000`
3. Установите зависимости: `npm install --production`
4. Соберите проект: `npm run build`
5. Запустите приложение

---

## 🔗 Шаг 4: Настройте домен

1. В настройках Node.js приложения добавьте ваш домен
2. Включите SSL (Let's Encrypt) в панели

---

## ✅ Готово!

Откройте `https://ваш-домен.ru` - сайт должен работать!

---

## 📖 Подробная инструкция

Смотрите файл **`REG_RU_DEPLOY.md`** для детальной инструкции.

---

## ❓ Проблемы?

1. Проверьте логи в панели REG.RU
2. Убедитесь, что приложение запущено
3. Проверьте, что все файлы загружены
4. Обратитесь в поддержку REG.RU


