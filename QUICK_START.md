# ⚡ Быстрый старт: Деплой на ваш хостинг

## 🎯 Что нужно сделать прямо сейчас:

### Шаг 1: Подготовьте проект (на вашем компьютере)

```bash
# Откройте терминал в папке проекта и выполните:
npm install
npm run build
```

Если сборка прошла успешно ✅ - проект готов!

---

### Шаг 2: Определите тип вашего хостинга

**Ответьте на вопросы:**

1. **Есть ли у вас доступ по SSH?** (можете подключиться через терминал к серверу)
   - ✅ Да → **Вариант A: VPS**
   - ❌ Нет → **Вариант B: Shared Hosting**

2. **Есть ли в панели хостинга раздел "Node.js" или "Приложения"?**
   - ✅ Да → **Вариант C: Node.js хостинг**

---

## 📋 Вариант A: VPS с SSH (самый простой)

### Что делать:

1. **Подключитесь к серверу:**
   ```bash
   ssh ваш_пользователь@IP_сервера
   ```

2. **Установите Node.js (если еще не установлен):**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

3. **Загрузите проект на сервер:**
   - Через Git: `git clone ваш-репозиторий.git`
   - Или через SFTP: загрузите все файлы (кроме node_modules, .next, .git)

4. **На сервере выполните:**
   ```bash
   cd путь/к/проекту
   npm install --production
   npm run build
   ```

5. **Установите PM2 и запустите:**
   ```bash
   sudo npm install -g pm2
   pm2 start npm --name "blck-ltd" -- start
   pm2 save
   pm2 startup
   ```

6. **Настройте Nginx:**
   - Создайте файл `/etc/nginx/sites-available/blck.ltd`
   - Скопируйте конфигурацию из `DEPLOY_INSTRUCTIONS.md`
   - Активируйте и перезагрузите Nginx

7. **Настройте SSL:**
   ```bash
   sudo certbot --nginx -d blck.ltd -d www.blck.ltd
   ```

**Готово!** Сайт должен работать на вашем домене.

---

## 📋 Вариант B: Shared Hosting (обычный хостинг)

### Что делать:

1. **Подготовьте файлы на компьютере:**
   ```bash
   npm install
   npm run build
   ```

2. **Загрузите через FTP/SFTP:**
   - Откройте FileZilla или WinSCP
   - Подключитесь к хостингу
   - Загрузите ВСЕ файлы проекта в папку `public_html` (или указанную хостингом)
   - **НЕ загружайте:** `node_modules`, `.next`, `.git`

3. **Если есть SSH доступ:**
   ```bash
   cd ~/public_html
   npm install --production
   npm run build
   npm start
   ```

4. **Если нет SSH, но есть панель управления:**
   - Найдите раздел "Node.js" или "Приложения"
   - Создайте новое приложение
   - Укажите путь к проекту
   - Команда запуска: `npm start`

5. **Настройте домен в панели хостинга**

**Готово!** Сайт должен работать.

---

## 📋 Вариант C: Node.js хостинг (с панелью)

### Что делать:

1. **Загрузите проект через FTP/SFTP** (см. Вариант B, шаг 2)

2. **В панели хостинга:**
   - Найдите раздел "Node.js" или "Приложения"
   - Нажмите "Создать приложение"
   - Заполните:
     - **Путь:** `/public_html` (или где загрузили файлы)
     - **Команда запуска:** `npm start`
     - **Node.js версия:** `18.x` или `20.x`
     - **Порт:** `3000` (или автоматически)

3. **Установите зависимости:**
   - В панели найдите кнопку "Установить зависимости"
   - Или выполните: `npm install --production`

4. **Соберите проект:**
   - В панели найдите кнопку "Собрать проект"
   - Или выполните: `npm run build`

5. **Запустите приложение:**
   - В панели нажмите "Запустить" или "Start"

6. **Добавьте домен:**
   - В настройках приложения найдите "Домены"
   - Добавьте ваш домен `blck.ltd`

**Готово!** Сайт должен работать.

---

## ❓ Нужна помощь?

1. **Откройте файл `DEPLOY_INSTRUCTIONS.md`** - там подробная инструкция
2. **Проверьте логи** на сервере или в панели хостинга
3. **Обратитесь в поддержку хостинга** - они помогут настроить

---

## ✅ Проверка после деплоя

1. Откройте `https://ваш-домен.com` в браузере
2. Проверьте все страницы сайта
3. Проверьте форму заказа

---

## 🎉 Удачи!

Если что-то не работает - напишите, помогу разобраться!


