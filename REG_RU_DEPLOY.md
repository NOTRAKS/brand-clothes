# 🚀 Деплой на REG.RU через ZIP архив

## 📦 Шаг 1: Подготовка ZIP архива

### Что ВКЛЮЧИТЬ в ZIP:
✅ **ВСЕ эти папки и файлы:**
- `app/` - все файлы
- `components/` - все файлы
- `public/` - все файлы (изображения)
- `styles/` - все файлы
- `lib/` - все файлы
- `hooks/` - все файлы
- `package.json`
- `package-lock.json`
- `next.config.mjs`
- `tsconfig.json`
- `postcss.config.mjs`
- `components.json`
- `.gitignore`

### ❌ Что НЕ включать в ZIP:
- `node_modules/` - НЕ загружайте!
- `.next/` - НЕ загружайте!
- `.git/` - НЕ загружайте!
- `pnpm-lock.yaml` - можно не включать
- Все файлы `.md` (инструкции) - можно не включать
- `deploy.sh`, `Dockerfile`, `nginx.conf` - можно не включать

### Как создать ZIP:

**На Mac:**
1. Выделите все нужные папки и файлы
2. Правый клик → "Сжать элементы"
3. Переименуйте архив в `blck-site.zip`

**На Windows:**
1. Выделите все нужные папки и файлы
2. Правый клик → "Отправить" → "Сжатая ZIP-папка"
3. Переименуйте архив в `blck-site.zip`

**Через терминал:**
```bash
# Перейдите в папку проекта
cd "/Users/gordei/Desktop/Новая папка 2"

# Создайте ZIP (исключая ненужные файлы)
zip -r blck-site.zip \
  app/ \
  components/ \
  public/ \
  styles/ \
  lib/ \
  hooks/ \
  package.json \
  package-lock.json \
  next.config.mjs \
  tsconfig.json \
  postcss.config.mjs \
  components.json \
  .gitignore \
  -x "*.DS_Store" "node_modules/*" ".next/*" ".git/*"
```

---

## 🌐 Шаг 2: Загрузка на REG.RU

### 2.1. Войдите в панель управления REG.RU

1. Откройте [https://www.reg.ru](https://www.reg.ru)
2. Войдите в аккаунт
3. Перейдите в раздел **"Хостинг"** или **"Веб-хостинг"**

### 2.2. Найдите раздел Node.js

1. В панели управления найдите раздел **"Node.js"** или **"Приложения"**
2. Если раздела нет - возможно нужен тариф с поддержкой Node.js
3. Обратитесь в поддержку REG.RU для активации Node.js

### 2.3. Загрузите ZIP архив

**Вариант A: Через файловый менеджер**
1. Откройте **"Файловый менеджер"** в панели
2. Перейдите в папку `public_html` (или указанную для домена)
3. Загрузите `blck-site.zip`
4. Распакуйте архив (обычно есть кнопка "Распаковать")

**Вариант B: Через FTP**
1. Используйте FileZilla или другой FTP клиент
2. Подключитесь к хостингу (данные в панели REG.RU)
3. Загрузите ZIP в `public_html`
4. Распакуйте через файловый менеджер в панели

**Вариант C: Через SSH (если есть доступ)**
```bash
# Подключитесь по SSH
ssh ваш_пользователь@ваш_домен.ru

# Перейдите в папку
cd ~/public_html

# Загрузите ZIP через SFTP или скопируйте
# Затем распакуйте:
unzip blck-site.zip
```

---

## ⚙️ Шаг 3: Настройка Node.js приложения в REG.RU

### 3.1. Создайте Node.js приложение

1. В панели REG.RU найдите раздел **"Node.js"** или **"Приложения"**
2. Нажмите **"Создать приложение"** или **"Добавить приложение"**
3. Заполните форму:

   **Основные настройки:**
   - **Название:** `blck-site` (любое)
   - **Путь к приложению:** `/public_html` (или где распаковали файлы)
   - **Node.js версия:** `18.x` или `20.x` (выберите доступную)
   - **Порт:** `3000` (или автоматически, если хостинг сам назначает)

   **Команды:**
   - **Команда установки зависимостей:** `npm install --production`
   - **Команда сборки:** `npm run build`
   - **Команда запуска:** `npm start`

   **Переменные окружения:**
   - `NODE_ENV=production`
   - `PORT=3000` (или порт, который назначил хостинг)

### 3.2. Установите зависимости

1. В настройках приложения найдите кнопку **"Установить зависимости"**
2. Или выполните команду в терминале/SSH:
   ```bash
   cd ~/public_html
   npm install --production
   ```

### 3.3. Соберите проект

1. В настройках приложения найдите кнопку **"Собрать проект"**
2. Или выполните команду:
   ```bash
   cd ~/public_html
   npm run build
   ```

### 3.4. Запустите приложение

1. В настройках приложения нажмите **"Запустить"** или **"Start"**
2. Дождитесь запуска (обычно 10-30 секунд)

---

## 🔗 Шаг 4: Настройка домена

### 4.1. Добавьте домен к приложению

1. В настройках Node.js приложения найдите раздел **"Домены"**
2. Добавьте ваш домен: `ваш-домен.ru`
3. Добавьте также: `www.ваш-домен.ru` (если нужно)
4. REG.RU автоматически настроит проксирование

### 4.2. Настройка DNS (если нужно)

Если домен не привязан автоматически:

1. В панели REG.RU перейдите в **"DNS"** или **"Управление DNS"**
2. Убедитесь, что домен указывает на ваш хостинг:
   - Тип: `A`
   - Имя: `@`
   - Значение: IP вашего хостинга (указан в панели)

---

## 🔒 Шаг 5: Настройка SSL (HTTPS)

### 5.1. Включите SSL в панели REG.RU

1. В панели найдите раздел **"SSL"** или **"Безопасность"**
2. Включите **"Let's Encrypt"** или **"Бесплатный SSL"**
3. Выберите домен `ваш-домен.ru`
4. Нажмите **"Установить"** или **"Активировать"**
5. Дождитесь установки (обычно 5-10 минут)

### 5.2. Проверьте HTTPS

1. Откройте `https://ваш-домен.ru` в браузере
2. Должен быть замочек в адресной строке ✅

---

## ✅ Шаг 6: Проверка работы

### Проверьте:

1. ✅ **Главная страница:** `https://ваш-домен.ru`
2. ✅ **Страница коллекций:** `https://ваш-домен.ru/collections`
3. ✅ **Страница заказа:** `https://ваш-домен.ru/checkout`
4. ✅ **Изображения загружаются** (проверьте все картинки)
5. ✅ **Форма заказа работает** (должен открываться Telegram)

---

## 🔧 Шаг 7: Управление приложением

### Полезные команды в панели REG.RU:

- **Перезапустить:** Нажмите "Перезапустить" в настройках приложения
- **Остановить:** Нажмите "Остановить"
- **Логи:** Найдите раздел "Логи" для просмотра ошибок

### Если есть SSH доступ:

```bash
# Посмотреть процессы
ps aux | grep node

# Посмотреть логи
tail -f ~/logs/node-app.log

# Перезапустить (если используете PM2)
pm2 restart blck-site
```

---

## ❓ Частые проблемы

### Сайт не открывается

1. **Проверьте, что приложение запущено:**
   - В панели REG.RU проверьте статус Node.js приложения
   - Должно быть "Запущено" или "Running"

2. **Проверьте логи:**
   - В панели найдите раздел "Логи"
   - Ищите ошибки (красным цветом)

3. **Проверьте порт:**
   - Убедитесь, что порт правильный (обычно 3000)
   - Проверьте, что хостинг не блокирует порт

### Ошибка "Cannot find module"

1. **Переустановите зависимости:**
   ```bash
   cd ~/public_html
   rm -rf node_modules
   npm install --production
   ```

2. **Проверьте package.json:**
   - Убедитесь, что все зависимости указаны

### Изображения не загружаются

1. **Проверьте папку public:**
   - Убедитесь, что папка `public/` загружена
   - Проверьте права доступа к файлам

2. **Проверьте пути:**
   - Все изображения должны быть в `public/`
   - Пути в коде: `/image.jpg` (начинается с `/`)

### Форма не работает / Telegram не открывается

1. **Проверьте HTTPS:**
   - Telegram требует HTTPS
   - Убедитесь, что SSL сертификат установлен

2. **Проверьте консоль браузера:**
   - Нажмите F12 в браузере
   - Ищите ошибки (красным цветом)

### Ошибка сборки

1. **Проверьте версию Node.js:**
   - Должна быть 18.x или выше
   - В панели выберите правильную версию

2. **Проверьте логи сборки:**
   - В панели найдите логи сборки
   - Исправьте ошибки

---

## 📞 Поддержка REG.RU

Если что-то не работает:

1. **Обратитесь в поддержку REG.RU:**
   - Чат в панели управления
   - Телефон: указан в панели
   - Email: support@reg.ru

2. **Что указать в обращении:**
   - Тип проблемы (сайт не открывается, ошибка сборки и т.д.)
   - Логи ошибок (если есть)
   - Скриншоты (если нужно)

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт должен работать на вашем домене!

**Проверьте:** `https://ваш-домен.ru`

---

## 📝 Чеклист деплоя

- [ ] ZIP архив создан (без node_modules, .next, .git)
- [ ] ZIP загружен на хостинг REG.RU
- [ ] Файлы распакованы в правильную папку
- [ ] Node.js приложение создано в панели
- [ ] Зависимости установлены (`npm install`)
- [ ] Проект собран (`npm run build`)
- [ ] Приложение запущено
- [ ] Домен добавлен к приложению
- [ ] SSL сертификат установлен
- [ ] Сайт открывается по HTTPS
- [ ] Все страницы работают
- [ ] Форма заказа работает


