# 📘 Подробная инструкция: Деплой на Vercel с доменом blck.ltd

## Шаг 1: Подготовка проекта

### 1.1. Проверьте, что проект готов к деплою

```bash
# В папке проекта выполните:
npm run build
```

Если сборка прошла успешно (без ошибок), можно продолжать.

### 1.2. Создайте Git репозиторий (если еще нет)

```bash
# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit"

# Создайте репозиторий на GitHub/GitLab/Bitbucket
# Затем подключите:
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

**Важно:** Vercel работает с Git репозиториями, поэтому проект должен быть в GitHub, GitLab или Bitbucket.

---

## Шаг 2: Регистрация на Vercel

### 2.1. Перейдите на сайт

Откройте [https://vercel.com](https://vercel.com)

### 2.2. Зарегистрируйтесь

1. Нажмите кнопку **"Sign Up"** (Войти)
2. Выберите способ регистрации:
   - **GitHub** (рекомендуется) - если проект на GitHub
   - **GitLab** - если проект на GitLab
   - **Bitbucket** - если проект на Bitbucket
   - **Email** - можно зарегистрироваться через email

3. Если выбрали GitHub/GitLab/Bitbucket:
   - Разрешите Vercel доступ к вашим репозиториям
   - Выберите, какие репозитории дать доступ (или все)

4. Если выбрали Email:
   - Введите email и пароль
   - Подтвердите email
   - Затем подключите Git репозиторий вручную

---

## Шаг 3: Подключение репозитория

### 3.1. Создайте новый проект

1. После входа в Vercel вы увидите дашборд
2. Нажмите кнопку **"Add New..."** или **"New Project"**

### 3.2. Импортируйте репозиторий

1. Вы увидите список ваших Git репозиториев
2. Найдите ваш проект в списке
3. Если проекта нет:
   - Нажмите **"Adjust GitHub App Permissions"** (если GitHub)
   - Или нажмите **"Import Git Repository"** и введите URL репозитория

4. Нажмите **"Import"** рядом с вашим проектом

### 3.3. Настройте проект

Vercel автоматически определит, что это Next.js проект. Проверьте настройки:

**Project Name:** `blck-ltd` (или любое другое имя)

**Framework Preset:** `Next.js` (должно определиться автоматически)

**Root Directory:** `./` (оставьте как есть, если проект в корне)

**Build Command:** `npm run build` (должно быть автоматически)

**Output Directory:** `.next` (должно быть автоматически)

**Install Command:** `npm install` (должно быть автоматически)

**Environment Variables:** (пока пропустите, если не нужны)

### 3.4. Деплой

1. Нажмите кнопку **"Deploy"**
2. Дождитесь завершения деплоя (обычно 1-3 минуты)
3. Вы увидите прогресс сборки в реальном времени

### 3.5. Первый деплой готов!

После успешного деплоя вы получите:
- **URL вида:** `blck-ltd.vercel.app` (или похожий)
- Сайт уже работает на этом адресе!

---

## Шаг 4: Добавление домена blck.ltd

### 4.1. Откройте настройки проекта

1. В дашборде Vercel найдите ваш проект
2. Нажмите на название проекта
3. Перейдите во вкладку **"Settings"** (Настройки)
4. В левом меню выберите **"Domains"** (Домены)

### 4.2. Добавьте домен

1. В поле **"Domain"** введите: `blck.ltd`
2. Нажмите **"Add"** или **"Add Domain"**

### 4.3. Vercel покажет инструкции по DNS

Vercel покажет, какие DNS записи нужно добавить. Обычно это:

**Вариант A (для корневого домена):**
```
Тип: A
Имя: @
Значение: 76.76.21.21
TTL: 3600 (или Auto)
```

**Вариант CNAME (для www):**
```
Тип: CNAME
Имя: www
Значение: cname.vercel-dns.com
TTL: 3600 (или Auto)
```

### 4.4. Настройте DNS у регистратора домена

1. Войдите в панель управления доменом (где покупали blck.ltd)
2. Найдите раздел **"DNS Management"** или **"DNS Records"**
3. Добавьте записи, которые показал Vercel:

   **Для blck.ltd (без www):**
   - Тип: `A`
   - Имя/Хост: `@` (или оставьте пустым)
   - Значение/IP: `76.76.21.21`
   - TTL: `3600`

   **Для www.blck.ltd:**
   - Тип: `CNAME`
   - Имя/Хост: `www`
   - Значение: `cname.vercel-dns.com`
   - TTL: `3600`

4. Сохраните изменения

### 4.5. Ожидание активации

1. Вернитесь в Vercel
2. Статус домена будет **"Pending"** (Ожидание)
3. Обычно активация занимает:
   - **5-15 минут** - если домен новый
   - **До 24 часов** - если домен уже использовался

4. Vercel автоматически проверит DNS и активирует домен
5. Когда статус изменится на **"Valid"** (Валидный) - домен готов!

### 4.6. SSL сертификат

Vercel **автоматически** выдаст SSL сертификат (HTTPS) для вашего домена. 
Ничего дополнительно настраивать не нужно!

---

## Шаг 5: Автоматический деплой

### 5.1. Как это работает

После первого деплоя Vercel настроит автоматический деплой:
- При каждом **push** в основную ветку (main/master) - автоматически задеплоится новая версия
- При создании **pull request** - создастся preview версия

### 5.2. Проверка автоматического деплоя

1. Внесите любое изменение в проект
2. Сделайте коммит и push:
   ```bash
   git add .
   git commit -m "Test auto deploy"
   git push
   ```
3. Вернитесь в Vercel
4. Вы увидите новый деплой в разделе **"Deployments"**
5. Деплой запустится автоматически!

### 5.3. Настройка веток (опционально)

По умолчанию деплоится ветка `main` или `master`. Чтобы изменить:
1. Settings → Git
2. Production Branch → выберите нужную ветку

---

## Шаг 6: Проверка работы

### 6.1. Проверьте сайт

1. Откройте `https://blck.ltd` в браузере
2. Убедитесь, что сайт открывается
3. Проверьте, что работает HTTPS (замочек в адресной строке)

### 6.2. Проверьте форму заказа

1. Перейдите на страницу товара
2. Нажмите "Купить"
3. Заполните форму на странице checkout
4. Нажмите "Перейти к оплате"
5. Должен открыться Telegram с предзаполненным сообщением

---

## 🔧 Полезные функции Vercel

### Просмотр логов

1. Откройте проект в Vercel
2. Вкладка **"Deployments"**
3. Нажмите на нужный деплой
4. Вкладка **"Functions"** или **"Logs"** - там логи

### Переменные окружения

Если понадобятся (например, для API ключей):
1. Settings → Environment Variables
2. Добавьте переменные
3. Передеплойте проект

### Analytics (опционально)

1. Settings → Analytics
2. Включите Vercel Analytics для статистики

---

## ❓ Частые проблемы

### Домен не активируется

**Проблема:** Домен долго в статусе "Pending"

**Решение:**
1. Проверьте DNS записи - они должны точно совпадать с инструкциями Vercel
2. Подождите до 24 часов (иногда DNS обновляется медленно)
3. Используйте [whatsmydns.net](https://www.whatsmydns.net) для проверки распространения DNS

### Ошибка сборки

**Проблема:** Деплой падает с ошибкой

**Решение:**
1. Проверьте логи в Vercel (Deployments → выберите деплой → Logs)
2. Убедитесь, что `npm run build` работает локально
3. Проверьте, что все зависимости в `package.json`

### Сайт не обновляется

**Проблема:** Изменения не появляются на сайте

**Решение:**
1. Убедитесь, что сделали `git push`
2. Проверьте, что деплой завершился успешно
3. Очистите кеш браузера (Ctrl+Shift+R)

---

## ✅ Чеклист готовности

- [ ] Проект собран без ошибок (`npm run build`)
- [ ] Проект в Git репозитории (GitHub/GitLab/Bitbucket)
- [ ] Зарегистрирован аккаунт на Vercel
- [ ] Проект подключен к Vercel
- [ ] Первый деплой успешен
- [ ] Домен blck.ltd добавлен в Vercel
- [ ] DNS записи настроены у регистратора
- [ ] Домен активирован (статус "Valid")
- [ ] Сайт открывается на https://blck.ltd
- [ ] Форма заказа работает корректно

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен на **https://blck.ltd** и будет автоматически обновляться при каждом изменении в репозитории!


