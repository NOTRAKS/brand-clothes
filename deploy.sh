#!/bin/bash

# Скрипт для подготовки проекта к деплою
# Использование: ./deploy.sh

echo "🚀 Подготовка проекта к деплою..."

# Проверка Node.js
if ! command -v node &> /dev/null; then
    echo "❌ Node.js не установлен. Установите Node.js 18+ и попробуйте снова."
    exit 1
fi

echo "✅ Node.js версия: $(node -v)"

# Установка зависимостей
echo "📦 Установка зависимостей..."
npm install

# Сборка проекта
echo "🔨 Сборка проекта..."
npm run build

if [ $? -eq 0 ]; then
    echo "✅ Проект успешно собран!"
    echo ""
    echo "📋 Следующие шаги:"
    echo "1. Загрузите файлы на хостинг через FTP/SFTP"
    echo "2. НЕ загружайте: node_modules, .next, .git"
    echo "3. На сервере выполните: npm install --production && npm run build && npm start"
    echo ""
    echo "📖 Подробная инструкция в файле DEPLOY_INSTRUCTIONS.md"
else
    echo "❌ Ошибка при сборке проекта!"
    exit 1
fi


