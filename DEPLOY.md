# Инструкция по деплою на blck.ltd

## Вариант 1: Vercel (Рекомендуется - самый простой)

Vercel создан разработчиками Next.js, поэтому это самый простой способ.

### 📘 Подробная пошаговая инструкция

**👉 Смотрите файл [VERCEL_DEPLOY.md](./VERCEL_DEPLOY.md) для детальной инструкции с объяснением каждого шага!**

### Краткая версия:

1. **Зарегистрируйтесь на [Vercel.com](https://vercel.com)** (можно через GitHub)

2. **Подключите репозиторий:**
   - Нажмите "Add New Project"
   - Подключите ваш GitHub/GitLab репозиторий
   - Или загрузите проект через CLI

3. **Настройте домен:**
   - В настройках проекта перейдите в "Domains"
   - Добавьте домен `blck.ltd`
   - Следуйте инструкциям по настройке DNS

4. **DNS настройки для blck.ltd:**
   ```
   Тип: A
   Имя: @
   Значение: 76.76.21.21
   
   Тип: CNAME
   Имя: www
   Значение: cname.vercel-dns.com
   ```

5. **Деплой:**
   - Vercel автоматически задеплоит при каждом push в репозиторий
   - Или используйте: `vercel --prod`

---

## Вариант 2: Обычный VPS/Хостинг (Node.js)

### Требования:
- Node.js 18+ 
- PM2 (для управления процессом)
- Nginx (для проксирования)

### Шаги:

1. **Подготовка проекта:**
   ```bash
   npm install
   npm run build
   ```

2. **Загрузите файлы на сервер:**
   - Через FTP/SFTP или Git
   - Исключите `node_modules` и `.next` (они установятся на сервере)

3. **Установите зависимости на сервере:**
   ```bash
   npm install --production
   npm run build
   ```

4. **Запустите с PM2:**
   ```bash
   npm install -g pm2
   pm2 start npm --name "blck-ltd" -- start
   pm2 save
   pm2 startup
   ```

5. **Настройте Nginx:**
   Создайте файл `/etc/nginx/sites-available/blck.ltd`:
   ```nginx
   server {
       listen 80;
       server_name blck.ltd www.blck.ltd;

       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

   Активируйте:
   ```bash
   ln -s /etc/nginx/sites-available/blck.ltd /etc/nginx/sites-enabled/
   nginx -t
   systemctl reload nginx
   ```

6. **Настройте SSL (Let's Encrypt):**
   ```bash
   certbot --nginx -d blck.ltd -d www.blck.ltd
   ```

7. **DNS настройки:**
   ```
   Тип: A
   Имя: @
   Значение: IP_вашего_сервера
   
   Тип: A
   Имя: www
   Значение: IP_вашего_сервера
   ```

---

## Вариант 3: Docker

Создайте `Dockerfile`:
```dockerfile
FROM node:18-alpine AS base

# Install dependencies only when needed
FROM base AS deps
WORKDIR /app
COPY package*.json ./
RUN npm ci

# Rebuild the source code only when needed
FROM base AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npm run build

# Production image
FROM base AS runner
WORKDIR /app
ENV NODE_ENV production
COPY --from=builder /app/public ./public
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static

EXPOSE 3000
ENV PORT 3000
CMD ["node", "server.js"]
```

Запуск:
```bash
docker build -t blck-ltd .
docker run -p 3000:3000 blck-ltd
```

---

## Быстрая проверка перед деплоем:

1. ✅ `npm run build` - должен пройти без ошибок
2. ✅ `npm start` - сайт должен открываться на localhost:3000
3. ✅ Все изображения в `/public` загружены
4. ✅ Форма checkout работает корректно

---

## После деплоя:

1. Проверьте работу сайта на `https://blck.ltd`
2. Проверьте форму заказа - должен открываться Telegram
3. Настройте мониторинг (опционально)

---

## Проблемы?

- **Сайт не открывается:** Проверьте DNS настройки (может занять до 24 часов)
- **Ошибка 500:** Проверьте логи на сервере
- **Изображения не загружаются:** Убедитесь, что папка `public` загружена
- **Telegram не открывается:** Проверьте, что используется `https://` (необходимо для Telegram)

